# ACG 记录 · 项目架构说明

## 1. 项目概览

「ACG 记录」是一个 Windows 桌面应用，用于记录番剧、漫画、轻小说与 Galgame 作品，支持筛选、统计、外部数据导入（MAL / Bangumi / VNDB）、设置定制与数据备份。单机本地运行，数据保存在本机 SQLite。

## 2. 技术栈

| 层 | 技术 |
| --- | --- |
| 桌面框架 | Tauri 2（Rust 后端 + 系统 WebView2） |
| 前端 | React 18 + TypeScript + Vite（HashRouter 路由） |
| 样式 | 原生 CSS，液态玻璃（Glassmorphism），CSS 变量主题 |
| 本地存储 | SQLite（tauri-plugin-sql）+ 引导文件 data_dir.json |
| 网络 | reqwest（系统代理优先 + 直连回退） |
| 打包 | NSIS 安装程序 + 便携版 exe |

## 3. 整体架构

```
┌─────────────────────────────────────────────┐
│  WebView 前端（React，src/）                 │
│  页面 → lib（db/api/settings/importers）     │
└──────────────┬──────────────────────────────┘
               │ Tauri invoke（JSON 命令）
┌──────────────▼──────────────────────────────┐
│  Rust 后端（src-tauri/src/lib.rs）           │
│  命令：搜索/封面/数据目录/备份/迁移           │
└──────────────┬──────────────────────────────┘
               │ SQLite / 文件系统 / HTTP API
┌──────────────▼──────────────────────────────┐
│  %APPDATA%\com.acg.tracker\（默认数据目录）   │
│  acg.db · covers\ · backgrounds\ · backups\  │
└─────────────────────────────────────────────┘
```

- **前端负责**：界面、状态、路由、调用后端命令、直接读写 SQLite（通过 sql 插件）。
- **后端负责**：HTTP 请求（Bangumi/VNDB）、文件复制/迁移、数据库快照备份、打开资源管理器。
- **数据目录**：默认为 `%APPDATA%\com.acg.tracker\`；可在设置页切换为自定义目录，切换时自动迁移数据库与封面。

## 4. 目录结构总览

```
D:\acg-tracker\
├── src\                   # 前端源码（React）
│   ├── main.tsx           # 入口
│   ├── App.tsx            # 应用外壳/路由/顶栏/背景
│   ├── styles.css         # 全部样式（主题变量、玻璃拟态）
│   ├── types.ts           # 核心数据模型
│   ├── lib\               # 数据/工具层
│   ├── components\        # 通用组件
│   └── pages\             # 页面
├── src-tauri\             # Rust 后端
│   ├── src\main.rs        # Windows 入口
│   ├── src\lib.rs         # 全部 Tauri 命令
│   ├── Cargo.toml         # Rust 依赖
│   ├── tauri.conf.json    # 应用/窗口/打包配置
│   ├── capabilities\      # 权限清单
│   ├── icons\             # 应用图标
│   └── target\            # 构建产物（可再生成）
├── outputs\               # 交付物（安装包/便携版/截图/文档）
├── dist\                  # 前端构建产物（可再生成）
├── node_modules\          # npm 依赖
├── package.json           # 前端脚本与依赖
└── README.md / 架构说明.md
```

## 5. 前端文件职责

### 入口与外壳

| 文件 | 作用 |
| --- | --- |
| `src/main.tsx` | React 挂载入口，引入全局样式 |
| `src/App.tsx` | 应用外壳：`SettingsProvider` + HashRouter；顶栏导航（首页/统计/导入/设置）；背景层（渐变光斑 + 自定义背景图）；全局「添加作品」弹窗；启动时自动备份；路由表（`/`、`/work/:id`、`/stats`、`/import`、`/settings`） |
| `src/styles.css` | 全部样式：CSS 变量（浅色/深色主题）、玻璃卡片、按钮/输入框/开关、作品网格、模态框、图表、设置页、导入页、背景图渲染 |

### 数据模型与工具层（lib/）

| 文件 | 作用 |
| --- | --- |
| `src/types.ts` | 核心类型：`Work`/`WorkInput`（作品字段）、`Category`/`Status`/`Season` 枚举、`BangumiItem`/`VndbItem`（API 搜索结果）、`ImportRow`（导入预览行） |
| `src/lib/db.ts` | SQLite 数据层：连接引导（先读 `data_dir.json` 决定数据库位置）、建表迁移（works/settings）、作品增删改查、设置键值读写、清空数据、`reloadDatabase()`、`works-changed` 事件广播 |
| `src/lib/settings.tsx` | 设置体系：`AppSettings` 类型与默认值、从数据库/引导文件加载、`SettingsProvider` + `useSettings()`、主题与卡片密度应用到 `<html>` 属性 |
| `src/lib/api.ts` | Rust 命令的 TypeScript 封装：Bangumi/VNDB 搜索、封面下载/保存、背景图保存、数据目录查询/打开、引导目录读写、目录迁移、数据库备份、`convertFileSrc` 资源 URL |
| `src/lib/events.ts` | 轻量全局事件：请求打开「添加作品」弹窗（供首页按钮与空状态触发） |
| `src/lib/constants.ts` | 类别/状态/季度/来源的中文标签与配色 |
| `src/lib/importers.ts` | MAL 导出 XML 解析、Bangumi CSV 解析（容错表头匹配）、标题规范化、按日期推断日本动画季度 |
| `src/lib/seed.ts` | 示例数据（首页空状态一键载入） |

### 通用组件（components/）

| 文件 | 作用 |
| --- | --- |
| `CoverImage.tsx` | 封面显示：本地路径走 asset 协议、远程 URL 直显、加载失败回退到类别占位 |
| `GlassModal.tsx` | 玻璃拟态模态框（遮罩、标题栏、内容区） |
| `EmptyState.tsx` | 空状态（无数据/无搜索结果） |
| `Charts.tsx` | 自绘条形图 `BarChart` 与环形图 `DonutChart`（无第三方图表库） |
| `WorkForm.tsx` | 添加/编辑作品表单：全部字段、封面选择（本地/URL）、外部链接、进度提示、支持 API 导入预填（`WorkFormPrefill`） |

### 页面（pages/）

| 文件 | 作用 |
| --- | --- |
| `HomePage.tsx` | 首页作品库：卡片网格；类别/年份/季度筛选；标题搜索；6 种排序；结果计数；空状态（载入示例数据） |
| `DetailPage.tsx` | 作品详情：大图、信息网格（年份/季度/评分/进度/时间）、简介、标签、笔记、外部链接、编辑与删除 |
| `StatsPage.tsx` | 统计：类别分布、状态环形图、年份分布、番剧季度分布 |
| `ImportPage.tsx` | 导入：文件导入（MAL XML / Bangumi CSV，预览 + 标题+年份去重）；API 搜索（Bangumi 类别筛选、VNDB，封面下载到本地，评分/总数量/标签预填） |
| `SettingsPage.tsx` | 设置：外观（主题/卡片密度/背景图片）、默认行为、导入、网络（代理模式/Bangumi·VNDB API 地址）、数据管理（数据目录切换、备份、导出/导入 JSON、清空）、关于 |

## 6. 后端文件职责

| 文件 | 作用 |
| --- | --- |
| `src-tauri/src/main.rs` | Windows 程序入口，调用 `acg_tracker_lib::run()` |
| `src-tauri/src/lib.rs` | 全部 Tauri 命令与内部逻辑（见下表） |
| `src-tauri/Cargo.toml` | Rust 依赖：tauri 2（protocol-asset）、tauri-plugin-sql/dialog/fs/opener、reqwest（json + system-proxy）、sqlx（sqlite） |
| `src-tauri/tauri.conf.json` | 应用标识符 `com.acg.tracker`、窗口尺寸、asset 协议（本地封面/背景图）、NSIS 打包配置 |
| `src-tauri/capabilities/default.json` | 前端可调用权限：sql、dialog、fs（读写文本）、opener |
| `src-tauri/icons/` | 应用图标 |

### lib.rs 命令清单

| 命令 | 作用 |
| --- | --- |
| `search_bangumi` | Bangumi 条目搜索（POST），支持类别 `filter.type`、数量上限、自定义 API 地址与代理；返回封面/评分（rating.score）/集数/卷数/标签 |
| `search_vndb` | VNDB 视觉小说搜索（POST /kana/vn），返回封面/评分（0-100）/无剧透标签 |
| `download_cover` | 把远程封面下载到数据目录 `covers\`（代理可配置） |
| `save_cover` | 把本地图片复制到 `covers\` |
| `save_background` | 把本地背景图复制到 `backgrounds\` |
| `get_data_dir` / `open_data_dir` | 返回默认数据目录 / 用资源管理器打开（支持自定义目录） |
| `get_bootstrap_data_dir` / `set_bootstrap_data_dir` | 读写引导文件 `data_dir.json`（记录自定义数据目录） |
| `ensure_data_dir` / `migrate_data_dir` | 创建目录 / 切换数据目录时迁移数据库（VACUUM INTO 快照）与封面 |
| `backup_database` | 备份数据库到 `backups\`（保留最近 N 份） |

内部辅助：`send_with_fallback`（按代理模式建客户端，失败回退直连）、`check_status`、`extract_bangumi_image`、`resolve_data_dir`、`current_db_path` 等；`#[cfg(test)] api_tests` 提供真实 API 回归测试。

## 7. 数据存储与设置体系

- **数据库**：默认 `%APPDATA%\com.acg.tracker\acg.db`（可在设置页切换目录）。表：
  - `works`：作品记录（标题、类别、年份/季度、状态、进度、双评分、简介、标签、笔记、封面、链接、来源、时间戳）
  - `settings`：键值设置（主题、默认值、网络、导入等）
- **文件**：`covers\`（封面）、`backgrounds\`（背景图）、`backups\`（自动/手动备份）、`data_dir.json`（自定义数据目录引导）
- **设置流程**：启动 → 读 `data_dir.json` 确定数据库位置 → 从 `settings` 表加载设置 → `SettingsProvider` 应用主题/密度到 `<html>`；修改设置即时写库并广播刷新。
- **数据变更刷新**：db.ts 的增删改会派发 `works-changed` 事件，各页面订阅后自动刷新。

## 8. 构建与发布

```bash
npm install          # 安装依赖
npm run tauri dev    # 开发运行（热更新）
npm run tauri build  # 打包（tsc + vite build + cargo release + NSIS）
cargo test           # 后端回归测试（含真实 API 调用）
```

发布产物（同步在 `outputs\`）：
- 安装程序：`src-tauri/target/release/bundle/nsis/ACG Tracker_0.1.0_x64-setup.exe`
- 便携版：`src-tauri/target/release/acg-tracker.exe`
- 说明文档：`README.md`、`架构说明.md`

> 注意：`src-tauri/target\`（尤其 `debug\`）与 `dist\` 均为可再生成构建产物，删除后可重新编译生成。