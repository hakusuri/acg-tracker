# 明日计划（2026-08-08）

## 1. 更改应用图标

- **现状**：使用 create-tauri-app 的默认 Tauri 图标（`src-tauri/icons/`）。
- **目标**：换成定制图标，建议「◆ ACG」玻璃质感风格，透明背景，主图 1024×1024 PNG。
- **步骤**：
  1. 生成/准备主图标 PNG（1024×1024）。
  2. 运行 `npx tauri icon <主图标.png>` 重新生成 `src-tauri/icons/` 全套（ico/icns/各尺寸 png）。
  3. `npm run tauri build` 重新打包。
  4. 更新 GitHub Release 附件（`gh release upload v0.1.0 <安装包> <便携版>` 或删除后重建）。
- **验证**：安装程序图标、任务栏图标、资源管理器图标均正常。

## 2. 修复「更改数据存放位置后更换背景图片无法显示」（猜测）

- **现象**：设置 → 数据管理 切换自定义数据目录后，更换/设置背景图片不显示。
- **根因假设（高概率）**：
  - `src-tauri/tauri.conf.json` 中 `app.security.assetProtocol.scope` 仅为 `"$APPDATA/**"`；
  - 自定义数据目录（如 `D:\xxx`）不在该范围内；
  - 前端用 `convertFileSrc(本地路径)` 加载背景图/封面时被 asset 协议拒绝 → 图片无法显示。
- **修复方向（推荐）**：
  1. Rust 新增命令（如 `allow_asset_dir`），调用 `app.asset_protocol_scope().allow_directory(dir, true)`（需 `use tauri::Manager;`）；
  2. 前端在启动读取到自定义 `dataDir` 后调用该命令，把当前数据目录加入资源协议白名单；切换数据目录后同样调用；
  3. 备选方案（不推荐）：把 scope 放宽为 `["$APPDATA/**", "**"]`，安全性较差。
- **验证**：
  1. 默认目录设置背景图 → 显示正常；
  2. 切换到自定义目录 → 重新设置背景图 → 显示正常；
  3. 重启应用后背景图仍显示（设置持久化）；
  4. 回归：自定义目录下 API 导入的封面也能正常显示。

## 收尾惯例

- 每次修改完成后删除 `src-tauri/target/debug` 中间产物。
- 完成后同步 Git（`git add -A && git commit && git push`）。
- 如需对外发布，更新 GitHub Release 附件。